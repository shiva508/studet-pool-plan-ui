import { DynamicHeigthDirectiveDirective } from './dynamic-heigth-directive.directive';

describe('DynamicHeigthDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHeigthDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
