import { DetterDynamicHeigthDirectiveDirective } from './detter-dynamic-heigth-directive.directive';

describe('DetterDynamicHeigthDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DetterDynamicHeigthDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
