import { DropdownDiectiveDirective } from './dropdown-diective.directive';

describe('DropdownDiectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownDiectiveDirective();
    expect(directive).toBeTruthy();
  });
});
